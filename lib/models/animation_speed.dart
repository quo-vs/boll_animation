class AnimationSpeed {
  double x;
  double y;

  AnimationSpeed({required this.x, required this.y});
}

enum MoveDirection {
  downLeft,
  downRight,
  upLeft,
  upRight,
  left,
  right,
  up,
  down
}